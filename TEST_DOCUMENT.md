# FastInOut 完整测试文档（详细版）

## 一、系统信息

| 项目 | 地址 |
|------|------|
| 前端 | http://localhost:3000 |
| 后端 | http://localhost:8000 |
| 管理员账号 | admin / admin123 |
| 数据库 | D:\A\claude\FastInOut\data\fastinout.db |

**测试前准备：**
1. 确保前后端都已启动
2. 用 admin / admin123 登录
3. 确保有基础数据：客户、供应商、商品、仓库（库存有货）

---

## 二、测试前数据准备

### 2.1 确保库存充足

访问 http://localhost:3000/inventory ，检查：
- 仓库1（主仓库）商品1（可口可乐）库存数量 > 10
- 仓库2（分仓库）商品1库存数量 > 10

**如果库存不足：**
1. 进入采购单 `/purchase-receipts`
2. 选择供应商、仓库
3. 添加商品1，数量10
4. 保存 → 审核（入库）
5. 库存会增加

### 2.2 快速清理测试数据（可选）

如果想从干净状态开始测试，备份并清空数据库：
```
D:\A\claude\FastInOut\data\fastinout.db
```

---

## 三、8个单据页面测试详解

### 3.1 销售订单 `/sales`

**打开页面：**
1. 侧边栏点击"销售" → "新增"
2. 或浏览器直接访问 http://localhost:3000/sales

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 制单人：admin（当前登录用户）
- 日期：今天日期
- 审核状态："待审核" 或 "待审核"文字
- 右上角：无印章
- 按钮：只显示 [保存] [新增] [关闭]
- 明细表格：5行空行
```

**Step 2：填写表单**
```
操作：
1. 选择客户（下拉框，选择第一个）
2. 选择仓库（下拉框，选择第一个）
3. 商品下拉框选择商品1
4. 修改数量为2
5. 修改单价为10

预期结果：
- 选择客户后：业务员自动填充（如果客户有默认业务员）
- 选择商品后：
  * 条形码自动显示
  * 单位自动显示
  * 单价自动填充为零售价
  * 金额自动计算 = 数量 × 单价 = 20
- 修改数量：金额自动重算
- 修改单价：金额自动重算
- 金额汇总区：
  * 商品金额 = 20
  * 优惠金额 = 0（可修改）
  * 优惠后金额 = 20
  * 待支付金额 = 20
```

**Step 3：保存**
```
操作：点击 [保存] 按钮

预期结果：
- 弹出提示："保存成功"
- 单据编号变为："XS2026xxxx-xxx"（自动生成的编号）
- 按钮变化：
  * [保存] 仍然显示
  * [审核] 出现
  * [打印] 出现
  * [复制] 出现
  * [新增] 仍然显示
  * [关闭] 仍然显示
- 右上角：仍然无印章
- 标签页：标题变为"销售订单 XS2026xxxx-xxx"
- 当前标签：保持蓝色高亮（不是变成灰色）
```

**Step 4：审核**
```
操作：点击 [审核] 按钮

预期结果：
- 弹出确认框："确认审核该订单？审核后将无法修改"
- 点击确定后：
  * 弹出提示："审核成功"
  * 右上角出现绿色"已审核"印章
  * 按钮变化：
    - [保存] 消失
    - [审核] 消失
    - [冲红] 出现（红色按钮）
    - [打印] 仍然显示
    - [复制] 仍然显示
    - [新增] 仍然显示
    - [关闭] 仍然显示
```

**Step 5：冲红**
```
操作：点击 [冲红] 按钮

预期结果：
- 弹出冲红原因输入框
- 输入原因："测试冲红"
- 点击确认：
  * 弹出提示："冲红成功"
  * 右上角印章变为红色"已冲红"
  * [冲红] 按钮消失
```

**Step 6：复制单据**
```
操作：点击 [复制] 按钮

预期结果：
- 新标签页打开
- 单据编号："（自动生成）"
- 表单数据清空（id清空、编号清空、状态=0）
- 原标签页不受影响
```

**Step 7：新增**
```
操作：点击 [新增] 按钮

预期结果：
- 新标签页打开
- 表单完全空白
- 5行空行
```

**Step 8：关闭**
```
操作：点击 [关闭] 按钮

预期结果：
- 当前标签关闭
- 切换到相邻标签
```

**常见问题排查：**
| 问题 | 可能原因 |
|------|---------|
| 保存后审核按钮不出现 | form.status 没有正确更新为0 |
| 保存后标签失去高亮 | updateCurrentTabPath 没有调用 router.replace |
| 保存时报500 | 后端报错，检查后端控制台 |
| 库存不足 | 商品库存为0，无法创建销售单 |

---

### 3.2 采购订单 `/purchases`

**打开页面：**
1. 侧边栏点击"采购" → "新增"
2. 或浏览器直接访问 http://localhost:3000/purchases

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 审核状态："待审核"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行
```

**Step 2：填写表单**
```
操作：
1. 选择供应商（下拉框）
2. 选择仓库（下拉框）
3. 商品下拉框选择商品1

预期结果：
- 选择商品后单价自动填充为成本价
- 金额自动计算
```

**Step 3：保存**
```
操作：点击 [保存]

预期结果：
- 弹出"保存成功"
- 单据编号变为："CG2026xxxx-xxx"
- 审核按钮出现
- form.status 更新为0
```

**Step 4：审核**
```
操作：点击 [审核]

预期结果：
- 弹出确认框
- 点击确定后弹出"审核成功"
- 右上角出现绿色印章
```

---

### 3.3 销售单 `/sales-deliveries`

**打开页面：**
1. 侧边栏点击"销售" → "销售单"
2. 或浏览器直接访问 http://localhost:3000/sales-deliveries

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行
```

**Step 2：填写表单**
```
操作：
1. 选择客户
2. 选择仓库
3. 添加商品1，数量5

预期结果：
- 金额自动计算
```

**Step 3：填写付款信息**
```
操作：
1. 现金金额输入100
2. 赊账金额应该自动计算 = 总金额 - 现金

预期结果：
- 赊账金额自动计算
- 注意：库存会被扣减
```

**Step 4：保存**
```
操作：点击 [保存]

预期结果：
- 弹出"保存成功"
- 单据编号变为："XS2026xxxx-xxx"
- 审核按钮出现
- 注意：此时不扣库存
```

**Step 5：审核（出库）**
```
操作：点击 [审核]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"审核成功"
  * 库存扣减（检查库存页面）
  * 客户应收增加
  * 右上角绿色印章
```

**Step 6：库存验证**
```
操作：进入库存页面 /inventory

预期结果：
- 仓库1 商品1 库存减少（如果库存不足会报错）
```

---

### 3.4 采购单 `/purchase-receipts`

**打开页面：**
1. 侧边栏点击"采购" → "采购单"
2. 或浏览器直接访问 http://localhost:3000/purchase-receipts

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行（注意：之前是1行，现在应该是5行）
```

**Step 2：填写表单**
```
操作：
1. 选择供应商
2. 选择仓库
3. 商品下拉框选择商品1
4. 单价填10

预期结果：
- 单价默认取成本价
- 金额自动计算
```

**Step 3：保存**
```
操作：点击 [保存]

预期结果：
- 弹出"保存成功"
- 单据编号变为："RK2026xxxx-xxx"
- 审核按钮出现
```

**Step 4：审核（入库）**
```
操作：点击 [审核]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"入库确认成功，库存已更新"
  * 库存增加
  * 供应商应付增加
  * 右上角绿色印章
```

---

### 3.5 退货订单 `/sales-returns`

**打开页面：**
1. 侧边栏点击"销售" → "退货订单"
2. 或浏览器直接访问 http://localhost:3000/sales-returns

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行
```

**Step 2：填写表单**
```
操作：
1. 选择客户
2. 选择仓库（可选）
3. 添加商品

预期结果：
- 金额自动计算
```

**Step 3：保存 → 审核**
```
操作：保存 → 审核

预期结果：
- 审核后状态变为1（已确认）
- 右上角绿色印章
```

---

### 3.6 采购退货订单 `/purchase-returns`

**打开页面：**
1. 侧边栏点击"采购" → "采购退货订单"
2. 或浏览器直接访问 http://localhost:3000/purchase-returns

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行
```

**Step 2：填写表单**
```
操作：
1. 选择供应商
2. 选择仓库（可选）
3. 添加商品

预期结果：
- 金额自动计算
```

**Step 3：保存 → 审核**
```
操作：保存 → 审核

预期结果：
- 审核后状态变为1（已确认）
```

---

### 3.7 退货单 `/return-deliveries`

**打开页面：**
1. 侧边栏点击"销售" → "退货单"
2. 或浏览器直接访问 http://localhost:3000/return-deliveries

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行
- 退货原因下拉框
```

**Step 2：填写表单**
```
操作：
1. 选择客户
2. 选择仓库（必填）
3. 退货原因选择"商品质量问题"
4. 添加商品

预期结果：
- 金额自动计算
```

**Step 3：保存**
```
操作：点击 [保存]

预期结果：
- 弹出"保存成功"
- 单据编号生成
- 仓管确认按钮出现
```

**Step 4：仓管确认**
```
操作：点击 [仓管确认]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"仓管确认成功，退货已入库"
  * 库存增加
  * 财务确认按钮出现
```

**Step 5：财务确认**
```
操作：点击 [财务确认]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"财务确认成功，已冲减客户应收"
  * 客户应收减少
  * 右上角印章更新
```

---

### 3.8 采购退货 `/purchase-return-deliveries`

**打开页面：**
1. 侧边栏点击"采购" → "采购退货"
2. 或浏览器直接访问 http://localhost:3000/purchase-return-deliveries

**Step 1：新建时的状态检查**
```
预期结果：
- 单据编号："（自动生成）"
- 按钮：[保存] [新增] [关闭]
- 明细表格：5行空行（注意：之前是1行，现在应该是5行）
- 退货原因下拉框
```

**Step 2：填写表单**
```
操作：
1. 选择供应商
2. 选择仓库
3. 退货原因选择
4. 添加商品

预期结果：
- 金额自动计算
```

**Step 3：保存**
```
操作：点击 [保存]

预期结果：
- 弹出"保存成功"
- 仓管确认按钮出现
```

**Step 4：仓管确认**
```
操作：点击 [仓管确认]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"仓管确认成功"
  * 库存减少
  * 财务确认按钮出现
```

**Step 5：财务确认**
```
操作：点击 [财务确认]

预期结果：
- 弹出确认框
- 点击确定后：
  * 弹出"财务确认成功"
  * 供应商应付减少
```

---

## 四、按钮状态完整对照表

### 4.1 销售订单 /purchases / sales-returns / purchase-returns

| 状态 | 保存 | 审核 | 冲红 | 打印 | 复制 | 新增 | 关闭 |
|------|------|------|------|------|------|------|------|
| 新建（无id） | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 草稿（id存在，status=0） | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 已确认（status=1） | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 已冲红（status=3） | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |

### 4.2 销售单 /purchase-receipts

| 状态 | 保存 | 审核 | 冲红 | 打印 | 复制 | 新增 | 关闭 |
|------|------|------|------|------|------|------|------|
| 新建（无id） | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 待处理（id存在，status=pending） | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 已出库/已入库（status=confirmed/已出库） | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 已冲红（status=reversed） | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |

### 4.3 退货单 /purchase-return-deliveries

| 状态 | 保存 | 仓管确认 | 财务确认 | 冲红 | 打印 | 复制 | 新增 | 关闭 |
|------|------|---------|---------|------|------|------|------|------|
| 草稿（status=0） | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 仓管已确认（status=2） | ❌ | ❌ | ✅ | ❌ | ✅ | ✅ | ✅ | ✅ |
| 财务已确认（status=3） | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 已冲红 | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |

---

## 五、印章显示规则

| 单据状态 | 印章显示 | 颜色 | 样式 |
|---------|---------|------|------|
| 新建（无id） | 无 | - | - |
| 草稿（status=0） | 无 | - | - |
| 已确认/已出库/已入库 | "已审核" | 绿色 | 右上角，半透明，旋转-15度 |
| 已冲红 | "已冲红" | 红色 | 右上角，半透明，旋转-15度 |

**检查方法：**
1. 打开任意单据新建页面 → 无印章
2. 保存后（草稿）→ 无印章
3. 审核后 → 绿色印章出现
4. 冲红后 → 红色印章替换绿色

---

## 六、标签页测试

### 6.1 多开同类型单据
```
操作：
1. 连续点击3次 [新增]（销售订单）

预期结果：
- 3个独立的销售订单标签页
- 每个标签数据独立
- 切换标签数据不丢失
```

### 6.2 保存后标签保持高亮
```
操作：
1. 打开销售订单
2. 填写表单
3. 保存

预期结果：
- 保存成功后，当前标签保持蓝色高亮
- 不是变成灰色需要重新点击
```

### 6.3 复制后新标签打开
```
操作：
1. 打开已有单据
2. 点击 [复制]

预期结果：
- 新标签页打开
- 原标签页不受影响
```

---

## 七、库存联动测试

### 7.1 销售单出库扣库存
```
操作：
1. 记录当前库存A
2. 创建销售单，添加商品1个
3. 审核
4. 查看库存

预期结果：库存从A变为A-1
```

### 7.2 采购单入库加库存
```
操作：
1. 记录当前库存A
2. 创建采购单，添加商品1个
3. 审核
4. 查看库存

预期结果：库存从A变为A+1
```

### 7.3 退货单入库加库存
```
操作：
1. 记录当前库存A
2. 创建退货单，添加商品1个
3. 仓管确认
4. 查看库存

预期结果：库存从A变为A+1
```

### 7.4 采购退货出库扣库存
```
操作：
1. 记录当前库存A
2. 创建采购退货，添加商品1个
3. 仓管确认
4. 查看库存

预期结果：库存从A变为A-1
```

### 7.5 冲红回滚库存
```
操作：
1. 创建销售单1个，审核（库存A→A-1）
2. 冲红该单据
3. 查看库存

预期结果：库存恢复到A
```

---

## 八、每页默认行数检查表

| 页面 | 路径 | 默认行数 | 检查项 |
|------|------|---------|--------|
| 销售订单 | /sales | 5行 | ✅ 打开应有5行空行 |
| 采购订单 | /purchases | 5行 | ✅ 打开应有5行空行 |
| 销售单 | /sales-deliveries | 5行 | ✅ 打开应有5行空行 |
| 采购单 | /purchase-receipts | 5行 | ✅ 打开应有5行空行 |
| 退货订单 | /sales-returns | 5行 | ✅ 打开应有5行空行 |
| 采购退货订单 | /purchase-returns | 5行 | ✅ 打开应有5行空行 |
| 退货单 | /return-deliveries | 5行 | ✅ 打开应有5行空行 |
| 采购退货 | /purchase-return-deliveries | 5行 | ✅ 打开应有5行空行 |

---

## 九、详细Bug报告模板

测试过程中发现bug请填写：

```
页面：________________
路径：________________
操作步骤：
1. ________
2. ________
3. ________

预期结果：_______________
实际结果：_______________
浏览器控制台报错：[有/无]
后端日志报错：[有/无]

截图：[粘贴截图]
```

---

## 十、常见错误排查

### 错误1：POST 500 Internal Server Error
**可能原因：**
1. 后端代码报错
2. 数据库字段不匹配
3. 编号重复

**排查方法：**
1. 打开浏览器F12 → Network
2. 查看POST请求的Response
3. 检查后端控制台报错信息

### 错误2：保存后审核按钮不出现
**可能原因：**
1. form.status 没有正确更新
2. 按钮v-if条件不满足

**排查方法：**
1. 打开浏览器F12 → Vue Devtools
2. 查看form.status的值
3. 应该是0（数字）而不是"0"（字符串）或"pending"

### 错误3：标签失去高亮
**可能原因：**
1. updateCurrentTabPath 后没有调用 router.replace
2. 路由没有更新

**排查方法：**
1. 保存后观察URL是否变化
2. 观察标签是否保持蓝色

### 错误4：库存不足
**可能原因：**
1. 之前测试扣光了库存
2. 商品没有入库

**解决方法：**
1. 创建采购单 → 审核（入库）
2. 或直接SQL更新库存：
```sql
UPDATE inventory SET quantity = 100 WHERE warehouse_id = 1 AND product_id = 1
```

---

## 十一、文件路径索引

### 后端单据处理
| 文件 | 负责单据 |
|------|---------|
| `D:\A\claude\FastInOut\server\routers\sales.py` | 销售订单、销售出库、销售退货 |
| `D:\A\claude\FastInOut\server\routers\purchases.py` | 采购订单、采购入库、采购退货 |
| `D:\A\claude\FastInOut\server\routers\sales_delivery.py` | 销售单（Phase A） |
| `D:\A\claude\FastInOut\server\routers\purchase_receipt.py` | 采购单（Phase A） |
| `D:\A\claude\FastInOut\server\routers\sales_return_dlv.py` | 退货单 |
| `D:\A\claude\FastInOut\server\routers\purchase_return_dlv.py` | 采购退货 |

### 前端单据页面
| 文件 | 负责页面 |
|------|---------|
| `D:\A\claude\FastInOut\pc\src\views\sales\Index.vue` | /sales |
| `D:\A\claude\FastInOut\pc\src\views\purchases\Index.vue` | /purchases |
| `D:\A\claude\FastInOut\pc\src\views\sales_delivery\Index.vue` | /sales-deliveries |
| `D:\A\claude\FastInOut\pc\src\views\purchase_receipt\Index.vue` | /purchase-receipts |
| `D:\A\claude\FastInOut\pc\src\views\sales\Returns.vue` | /sales-returns |
| `D:\A\claude\FastInOut\pc\src\views\purchases\Returns.vue` | /purchase-returns |
| `D:\A\claude\FastInOut\pc\src\views\sales_return_dlv\Index.vue` | /return-deliveries |
| `D:\A\claude\FastInOut\pc\src\views\purchase_return_dlv\Index.vue` | /purchase-return-deliveries |

### 通用组件
| 文件 | 说明 |
|------|------|
| `D:\A\claude\FastInOut\pc\src\views\Layout.vue` | 标签页管理 |
| `D:\A\claude\FastInOut\pc\src\components\DocumentStamp.vue` | 印章组件 |
| `D:\A\claude\FastInOut\pc\src\components\ReverseDialog.vue` | 冲红弹窗 |
